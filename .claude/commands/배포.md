---
description: 변경사항 커밋하고 Netlify 배포
---

# 배포 명령어

다음 단계를 순서대로 실행:

1. `git status`로 변경사항 확인
2. 변경된 파일 모두 `git add`
3. 적절한 커밋 메시지로 `git commit`
4. `git push`로 원격 저장소에 푸시
5. Netlify 자동 배포 확인 (push하면 자동 배포됨)
6. 완료되면 사용자에게 알림
